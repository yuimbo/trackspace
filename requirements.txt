flask>=3.0
mutagen>=1.47
python-dotenv>=1.0
numpy>=1.24
torch>=2.0
transformers>=4.40
librosa>=0.10
# madmom builds from source unless a wheel exists; if pip fails: ``pip install cython && pip install madmom --no-build-isolation``
cython>=3.0.10
madmom>=0.16.1
scikit-learn>=1.3
watchdog>=4.0
onnxruntime>=1.17

# Apple Silicon — Metal-accelerated t-SNE (optional; ``backend.embeddings``).
mlx-vis>=0.7.0; sys_platform == "darwin" and platform_machine == "arm64"