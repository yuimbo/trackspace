{
  "name": "trackspace-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently --names backend,frontend --prefix-colors cyan,magenta \"../.venv/bin/python ../app.py ${ROOT:-.}\" \"vite\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "vite": "^6.2"
  }
}
